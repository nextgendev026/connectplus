# secrets — never commit (the example file stays tracked)
.env*
!.env.example

# prisma / runtime
node_modules
.next
prisma/*.db
prisma/*.db-journal
*.log

# user uploads (keep the placeholder so the dir survives a clone)
public/uploads/*
!public/uploads/.gitkeep

# freebuff local tooling workspace (scratch scripts + env backups)
.freebuff/

# misc
*.tsbuildinfo
next-env.d.ts
.vercel
.DS_Store
coverage
test-results
.test-results
playwright-report
blob-report
.vscode
.idea
